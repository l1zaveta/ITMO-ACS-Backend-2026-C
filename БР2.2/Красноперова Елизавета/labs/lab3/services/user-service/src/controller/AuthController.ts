import {Request,Response} from "express"; import bcrypt from "bcryptjs"; import jwt from "jsonwebtoken"; import {AppDataSource} from "../index"; import {User} from "../entity/User";
const repo=()=>AppDataSource.getRepository(User); const secret=()=>process.env.JWT_SECRET||"your-secret-key-change-in-production";
export class AuthController{
 static register=async(req:Request,res:Response)=>{try{const {username,email,password}=req.body;if(!username||!email||!password)return res.status(422).json({error:{code:"VALIDATION_ERROR",message:"Необходимо заполнить username, email и password"}});const ex=await repo().findOne({where:[{username},{email}]});if(ex)return res.status(409).json({error:{code:"CONFLICT",message:"Username или email уже занят"}});const u=repo().create({username,email,password_hash:await bcrypt.hash(password,10)});await repo().save(u);const token=jwt.sign({userId:u.user_id},secret(),{expiresIn:"7d"});return res.status(201).json({token,user:{user_id:u.user_id,username:u.username,avatar_url:u.avatar_url,bio:u.bio}});}catch{return res.status(400).json({error:{code:"VALIDATION_ERROR",message:"Ошибка валидации"}});}};
 static login=async(req:Request,res:Response)=>{try{const {email,password}=req.body;const u=await repo().findOne({where:{email}});if(!u||!(await bcrypt.compare(password,u.password_hash)))return res.status(401).json({error:{code:"UNAUTHORIZED",message:"Неверный email или пароль"}});const token=jwt.sign({userId:u.user_id},secret(),{expiresIn:"7d"});return res.json({token,user:{user_id:u.user_id,username:u.username,avatar_url:u.avatar_url,bio:u.bio}});}catch{return res.status(400).json({error:{code:"VALIDATION_ERROR",message:"Ошибка валидации"}});}};
}
